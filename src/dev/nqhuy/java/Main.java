package dev.nqhuy.java;public class Main {    public static void main(String[] args) {        /*        * Refection        * */        System.out.println("A-- Reflection--------");        // Bài 1: Hiện thị thông tin Object        // Hiển thị thông tin 1 class bất kỳ        // Hiện thị ra các thuộc tính của nó        Employ huy = new Employ("Nguyễn Huy", 25);        Common.displayInfo(huy);        // Bài 2: Sao chép giá trị object        System.out.println("-----------------------");        Employ a = new Employ("Gia An", 1);        Employ b = new Employ();        // Bị tham chiếu: nếu gán như thế thì nó gán địa chỉ ô nhớ        // tức là nếu ông a hoạc b thay đổi thì là thằng còn lại cũng thay đổi theo        // Tránh tham chiếu: gán từng thuộc tính của a cho b => tạo ra 1 ô nhớ khác nhau        // a hoặc b thay đổi thì không anh hưởng gid ến đối ttượng còn lại        Common.copyObject(a, b);        Common.displayInfo(b);        // b sẽ có dữ liệu và nso độc lập với a, giữ liệu có a thay đổi thì khoogn liên quan đến b nữa         /*        * Generic        * */        System.out.println("B-- Generic--------");        Common<Employ> cm = new Common<Employ>();        cm.displayInfo2(a);        Employ c = new Employ();        System.out.println("------ c--------");        Common.copyObject(huy, c);        Common.displayInfo(c);        new Common<String>().unitTest(huy.getName());    }}